type Link {
    _id: ID!
    marketFqdn: String!
    productPath: String!
    productParameter: String
    productUrl: String!
    shopInLinkPath: String!
    owner: ID!
    visitors: Int!
    expiresAt: String
    expiresView: Int
    createdAt: String!
    updatedAt: String!
}
